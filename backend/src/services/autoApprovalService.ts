export function startAutoApprovalJob() {
  // No-op: 24-hour auto-approval has been disabled.
  // Only Open House bookings are auto-approved at creation time.
  // All other booking types require manual admin approval.
}
